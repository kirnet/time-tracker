info_topic:
    channel: info/channel
    handler:
        callback: 'info.topic' #related to the getName() of your topic

info_rpc:
    channel: sample/{method}
    handler:
        callback: 'info.rpc' #related to the getName() or your RPC service
    requirements:
        method:
            pattern: "[a-z_]+"