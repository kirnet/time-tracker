{% extends 'base.html.twig' %}

{% block title %}New Timer{% endblock %}

{% block body %}
    <h1>Create new Timer</h1>

    {{ include('timer/_form.html.twig') }}

    <a href="{{ path('timer_index') }}">back to list</a>
{% endblock %}
