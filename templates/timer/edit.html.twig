{% extends 'base.html.twig' %}

{% block title %}Edit Timer{% endblock %}

{% block body %}
    <h1>Edit Timer</h1>

    {{ include('timer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('timer_index') }}">back to list</a>

    {{ include('timer/_delete_form.html.twig') }}
{% endblock %}
